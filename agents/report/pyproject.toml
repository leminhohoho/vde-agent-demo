[project]
name = "vdagent-report"
version = "0.1.0"
description = "vdagent report agent (built from agents/_template; LiteLLM tool loop)"
requires-python = ">=3.12,<3.13"
dependencies = [
  "vdagent-proto",
  "grpcio>=1.66",
  "grpcio-health-checking>=1.66",
  "python-dotenv>=1.0",
  "litellm>=1.50",
  "mcp>=1.9",
]

[tool.uv.sources]
vdagent-proto = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vdagent_report"]
